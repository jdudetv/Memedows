(process.env as any).FLUENTFFMPEG_COV = "";

// const util = require("util");
// import timers from "timers";
// console.log("Builtin",util.inspect(global.setInterval))
// console.log("Timer",util.inspect(timers.setInterval))
// global.setInterval =  timers.setInterval as any;
// global.setTimeout = global.setTimeout || timers.setTimeout as any;

export {};
